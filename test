#!/usr/bin/env bash

# Run all difz tests in Debug mode through the canonical Nix check.

set -u

system="$(nix eval --impure --raw --expr builtins.currentSystem)"
nix build -L ".#checks.${system}.test"
